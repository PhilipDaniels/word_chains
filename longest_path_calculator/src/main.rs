fn main() {

}

fn load_graphs() {

}

/*
Create all graphs for length N from longest component only
Sort graphs by increasing component size so that we handle the smallest ones first
Determine completed words by scanning output\chainsNN files and calculate remaining words

let current_long_path = [];
foreach word still to be completed
    let path = calculate the longest path()
    write output\chainsNN\word.txt file
    if path.len > current_long_path.len then
        current_long_path = path
        print current_long_path

scan files in directory chainsNN
find the file with the longest number of words (split by space)
write file chainsNN\00_longest_path.txt
*/
